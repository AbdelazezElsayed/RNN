# RNN
Recurrent Neural Network (RNN) in Python using NumPy. The model is trained to predict the fourth word in a 4-word sequence. It demonstrates how feedforward and backpropagation work in a minimal RNN setup without using any external machine learning libraries.
